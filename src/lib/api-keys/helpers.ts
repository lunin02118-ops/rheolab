// API key helpers — getActiveGroqKey removed in S-1 security fix.
// AI keys are now resolved server-side by the Rust parsing command.
// This file is intentionally empty; it can be deleted once all
// references in test mocks are cleaned up.
