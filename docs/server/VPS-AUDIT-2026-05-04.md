# VPS Audit — 2026-05-04

Scope: configured RheoLab VPS `license.vizbuka.ru` / `vm3776683.firstbyte.club`.

Mode: read-only audit. No cleanup, package upgrade, service restart, config edit, or deletion was performed.

Local evidence artifacts:

- `outputs/server-audit/vps-audit-20260504-093724.json`
- `outputs/server-audit/vps-audit-followup-20260504-093835.json`

## Executive Summary

The VPS is operational, but the root disk is tight and backup retention needs attention.

- OS: Ubuntu 24.04.4 LTS, kernel `6.8.0-106-generic`.
- Uptime: 3 weeks, 1 day.
- Root disk: `5.8G` total, `4.5G` used, `1.3G` free, `78%` used.
- RAM: `709MiB` total, `326MiB` used, `382MiB` available.
- Swap: `512MiB` total, `348MiB` used.
- Web stack: Apache 2.4.58, PHP 8.3.6, MySQL 8.0.45.
- Docker: not installed / not used.
- Public ports: `22`, `80`, `443`.
- MySQL listens on localhost only: `127.0.0.1:3306`, `127.0.0.1:33060`.

Primary risk: local license-server backups take `1.7G` on a `5.8G` root disk. A backup already failed on 2026-05-01 with `No space left on device`, then recovered on 2026-05-02 through 2026-05-04.

## Services

Running key services:

- `apache2.service`
- `mysql.service`
- `cron.service`

Failed units:

- `rc-local.service` is failed. This may be harmless if unused, but it should be reviewed.

Apache vhosts:

- `license.vizbuka.ru`
- `rheolab.site`
- `www.rheolab.site`

TLS:

- `license.vizbuka.ru` certificate valid until 2026-06-11.
- `rheolab.site` / `www.rheolab.site` certificate valid until 2026-06-11.
- `certbot.timer` is enabled.

## CPA / Hermes

No CPA or Hermes runtime was found on this VPS:

- no matching systemd units;
- no matching running processes;
- no matching Debian packages;
- no matching directories under `/var/www`, `/opt`, `/srv`, `/etc`, `/usr/local/bin`;
- no matching Apache site references.

Decision: no CPA/Hermes update can be performed on this server unless they use different service names or live on another VPS.

## Disk Usage

Largest roots:

- `/var`: `2.7G`
- `/usr`: `1.3G`
- `/root`: `24M`

Important `/var` users:

- `/var/backups/license-server`: `1.7G`
- `/var/cache`: `223M`
- `/var/lib/mysql`: `194M`
- `/var/log/journal`: `95M`
- `/var/log/btmp`: `39M`
- `/var/log/apache2`: `13M`
- `/var/www/license-server`: `85M`
- `/var/www/rheolab.site`: `2.6M`

Website deploy backups under `/var/www/rheolab.site_backup_*` are numerous but individually small. Total impact is modest compared to license backups, but they are safe candidates for retention pruning after confirmation.

## Backups

Root crontab:

- `03:00` daily: `/usr/local/bin/backup-license.sh`
- `03:30` daily: `/usr/local/bin/cleanup-license.sh`

Backup status:

- Latest local backup: `/var/backups/license-server/backup_2026-05-04_03-00-01.tar.gz`
- Latest archive integrity: `gzip -t` passed.
- Latest archive contains:
  - `database.sql.gz`
  - `files.tar.gz`
- S3 upload reported successful for 2026-05-02, 2026-05-03, and 2026-05-04.

Known issue:

- 2026-05-01 backup had a `No space left on device` failure during final archive creation.
- A leftover staging directory exists for `2026-05-01_03-00-01` with `files.tar.gz` around `356M`.

Current local backup footprint:

- `/var/backups/license-server`: `1.7G`
- Older local archives from 2026-04-25 through 2026-04-30 account for roughly `1.06G`.
- Failed/staging 2026-05-01 directory accounts for roughly `356M`.

Recommendation: reduce local retention from 30 days to a smaller value, such as 7 days or latest 3 archives, because off-server S3 upload exists and the VPS disk is small.

## Cache / Log Cleanup Candidates

Safe candidates after explicit confirmation:

- apt cache: about `214M`.
- system journal: about `95M`.
- `/var/log/btmp`: about `39M`.
- old website deploy backups: roughly tens of MB.
- failed license backup staging directory from 2026-05-01: about `356M`.
- older local license backup archives after confirming S3 presence: roughly `1.06G` for 2026-04-25 through 2026-04-30.

Do not delete without confirmation:

- current `/var/www/license-server`;
- current `/var/www/rheolab.site`;
- latest local license backups;
- MySQL data;
- TLS material;
- application keys/config.

## Package Updates

Current package cache reports upgradable packages:

- 13 packages would upgrade in a normal `apt-get upgrade` simulation.
- 15 packages are deferred due to Ubuntu phasing, including PHP 8.3 packages and netplan packages.
- `linux-image-virtual` is kept back.

Notable upgrade candidates:

- `coreutils`
- `tzdata`
- `pollinate`
- `binutils*`
- `linux-libc-dev`
- `ubuntu-drivers-common`

Recommendation:

- Safe maintenance window: run normal `apt-get upgrade` for the 13 non-held packages.
- Do not force phased PHP updates unless there is a security advisory or a planned maintenance window.
- Kernel package requires separate handling and likely reboot planning.

## Security Notes

SSH effective settings:

- `PermitRootLogin yes`
- `PasswordAuthentication yes`
- `PubkeyAuthentication yes`
- `X11Forwarding yes`

Fail2ban is active:

- jail: `sshd`
- total failed: `51534`
- total banned: `8622`
- currently banned: `6`

Recommendation:

- After confirming key-based access from at least one backup terminal/session, disable SSH password auth and consider `PermitRootLogin prohibit-password` or a non-root deploy user.
- Disable X11 forwarding if not needed.

## Recommended Next Actions

P0, confirm before execution:

1. Remove the failed 2026-05-01 staging backup directory.
2. Clean apt cache.
3. Vacuum journal to a bounded retention window.
4. Truncate `/var/log/btmp`.

P1, confirm after checking S3 availability:

1. Delete older local license backup archives from 2026-04-25 through 2026-04-30.
2. Change local backup retention from 30 days to 7 days or latest 3 archives.

P2:

1. Run normal package upgrade for the 13 non-held packages.
2. Review `rc-local.service`.
3. Harden SSH auth settings.

No action:

- CPA/Hermes update: no matching services/packages/apps were found on this VPS.
