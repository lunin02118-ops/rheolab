# RheoLab Enterprise 0.2.3-alpha.23

Published: 2026-06-12
Channel: alpha

## Scope

`0.2.3-alpha.23` refines the new `О программе` dialog after UI review:
the dialog now has three explicit tabs, the old duplicate About block is
removed from settings, and the contact email is consolidated to
`info@rheolab.site`.

## Provenance

- Source/release commit: `95ce520` (`fix(ui): Consolidate about and update controls`)
- Published version: `0.2.3-alpha.23`
- Installer: `RheoLab Enterprise_0.2.3-alpha.23_x64-setup.exe`
- Installer SHA-256: `a5f2415521340f78cb7954b62efafcfa5a9bf1afb3a4e8c750826d3840186a02`
- Alpha manifest: `https://license.vizbuka.ru/releases/v1/update/windows-x86_64/alpha.json`
- Artifact URL: `https://license.vizbuka.ru/releases/artifacts/0.2.3-alpha.23/RheoLab%20Enterprise_0.2.3-alpha.23_x64-setup.exe`

## Validation

- `npm run test -- tests/components/AboutProgramDialog.test.tsx tests/components/DashboardLayoutClient.test.tsx`
- `npm run typecheck`
- `npm run lint`
- `npm run release:prepare -- --channel alpha`
- Release gate: passed, 7 exports / 4 fixtures / 4 settings phases.
- `node scripts/deploy/publish-update.js --channel alpha --key C:\Users\VladimirWorkPC\.ssh\rheolab_deploy`
- `python scripts/dev/verify-alpha-pipeline.py --expect-version 0.2.3-alpha.23`
- `npm run server:doctor`

Post-deploy endpoint smoke reported `31 checks passed`.

## Channel Result

- Superuser alpha clients with a valid `X-Update-Token` receive `0.2.3-alpha.23`.
- Requests without the alpha HMAC token are downgraded to stable and receive
  `0.2.3-alpha.19`.
- Standard / enterprise / trial / demo users remain on the stable channel.

## UI Result

- Header `О программе` opens the `Обновления` tab.
- Trial activation CTA opens the `Лицензия` tab.
- The third tab, `Контакты`, contains contact rows, website/MAX links, training
  video link, and QR codes.

## Known Notes

- The update tab shows the build metadata available inside the application:
  app version, channel, build date, and commit hash. The installer SHA-256 is
  recorded in this release note and in the deployed channel manifest.
- The release-gate teardown printed a Windows `EPERM` cleanup warning for an
  isolated WebView2 temp directory after the test process exited; the gate
  itself passed and returned exit 0.
