# Agent progress report

Branch: audit/baseline-command-matrix
Validated PR head: 18e81504104ecb36220ac9c1d729243225dec620
PR: #5 https://github.com/lunin02118-ops/rheolab/pull/5
Phase: PR-B - Baseline audit and quality gate inventory
Date: 2026-06-13
Agent: Codex

---

## Objective

Add a documentation-only baseline report for the audit/refactoring split work.

This PR records command outcomes, missing gates, and local environment gaps observed during
baseline execution. It does not change runtime code, scripts, tests, generated bindings, IPC
contracts, or Rust/Tauri source.

---

## Changes

- Added `docs/audits/progress/2026-06-13-baseline.md`.
- Recorded initial baseline command observations.
- Recorded follow-up staging observations separately from this PR scope.
- Documented unavailable license-server Composer gates.
- Documented lint/typecheck exclusions and subproject gate gaps.

Audit runner hardening is intentionally outside this PR and is reviewed in PR #6.
Rust Clippy cleanup is intentionally outside this PR and is reviewed in PR #7.

---

## Files changed

- `docs/audits/progress/2026-06-13-baseline.md`

---

## Behavior changes

None / documentation-only.

This PR does not change application runtime behavior, audit script behavior, Rust code,
generated TypeScript bindings, Tauri IPC registration, or release behavior.

---

## Command matrix

Raw logs were written under `runtime/audit/baseline-2026-06-13/` during the original local
baseline run and are intentionally not committed.

### Initial baseline observations

These rows describe the starting point that the baseline report captured. They are observations,
not changes made by PR #5.

| Command | Exit code | Observation |
|---|---:|---|
| `npm ci` | 0 | Installed root dependencies. The full dependency tree reported 2 high severity vulnerabilities. |
| `npm run lint` | 0 | ESLint passed. |
| `npm run typecheck` | 0 | `tsc --noEmit` passed. |
| `npm test` | 0 | Vitest passed: 112 files, 1513 tests. Existing React `act(...)` warnings appeared in component tests. |
| `npm run test:release-gate` | 0 | Release gate passed: 1 Playwright/Tauri test, 7 exports, 4 fixtures. Cleanup warned about a WebView2 temp directory `EPERM`. |
| `npm run audit:frontend-ipc` | 124 | Timed out after 304s in the deep audit E2E phase and left child processes that had to be stopped manually. |
| `npm run audit:large-ipc` | 0 | Scanned 93 Rust files. No large-IPC contract violations were found. |
| `cargo test --manifest-path src-tauri/Cargo.toml -- --test-threads=1` | 101 | Reproduced a Windows executable lock on `src-tauri/target/debug/rheolab-enterprise.exe` (`os error 5`) after Tauri/E2E activity. |
| `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings` | 101 | Failed on existing Clippy findings, including `doc_overindented_list_items`, `manual_repeat_n`, `clone_on_copy`, `too_many_arguments`, `filter_map_bool_then`, `unnecessary_sort_by`, and `useless_vec`. |
| `npm --prefix website ci` | 0 | Installed website dependencies. The website dependency tree reported 12 vulnerabilities: 1 low, 2 moderate, 9 high. |
| `npm --prefix website run build` | 0 | Astro build passed and generated 18 pages. |
| `composer --working-dir=license-server validate` | 127 | Composer was not available in PATH. |
| `composer --working-dir=license-server test` | 127 | Composer was not available in PATH. |
| `npm run version:validate` | 0 | Version SSoT validation passed in the local baseline environment. |
| `npm audit --omit=dev` | 0 | Production npm dependency audit found 0 vulnerabilities. |
| `cargo audit` from `src-tauri/` | 0 | RustSec scan completed successfully. |

### Post-cleanup observations from staging work

These rows document follow-up staging outcomes so reviewers can understand the baseline trend.
They are not part of PR #5 implementation scope.

| Area | Follow-up observation | Reviewed in |
|---|---|---|
| Frontend IPC audit runner | Timeout and cleanup paths were hardened in the staging work. Static/reporting and short timeout smoke runs passed after that change. | PR #6 |
| Rust test gate after cleanup | `cargo test --manifest-path src-tauri/Cargo.toml -- --test-threads=1` later passed after process cleanup and Rust cleanup work. | PR #6 / PR #7 |
| Rust Clippy gate after cleanup | `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings` later passed after Clippy cleanup edits. | PR #7 |
| TypeScript gate after generated binding comment update | `npm run typecheck` later passed after staging cleanup. | PR #7 |

PR #5 does not contain audit runner hardening, Clippy cleanup, generated binding changes, or
Rust source edits. Those diffs are intentionally isolated in later split PRs.

### Validation for this split PR

These commands validate the current PR #5 diff itself.

| Command | Exit code | Result |
|---|---:|---|
| `git diff --check` | 0 | Pass. No whitespace errors. |
| Hidden/bidirectional Unicode scan over `.github/PULL_REQUEST_TEMPLATE` and `docs/audits` | 0 | Pass. No hidden/control/bidi-control characters, zero-width marks, bidi isolates/overrides, or BOM found. |
| `npm run version:validate` | 0 | Pass. Version SSoT remains consistent. |

---

## Skipped / unavailable gates

| Command | Status | Reason | Follow-up |
|---|---|---|---|
| `composer --working-dir=license-server validate` | unavailable | Composer is not installed or not available in PATH on this machine. | Install Composer and define/confirm license-server validation scripts before making this a required local gate. |
| `composer --working-dir=license-server test` | unavailable | Composer is not installed or not available in PATH on this machine. | Install Composer and define/confirm license-server test scripts before making this a required local gate. |

---

## Test failures / deviations

- Initial `audit:frontend-ipc` timed out in the baseline run and exposed process cleanup risk.
  The fix is not in this PR; it is reviewed in PR #6.
- Initial `cargo test` was blocked by a Windows executable lock after Tauri/E2E activity.
  Follow-up cleanup observations are recorded above, but the implementation is not in PR #5.
- Initial `cargo clippy -D warnings` failed on existing lint debt. The cleanup is not in this PR;
  it is reviewed in PR #7.
- Composer-based license-server checks are unavailable in this environment because `composer` is
  not installed or not in PATH.

---

## Script and subproject inventory

Root `package.json` contains the expected baseline scripts:

- `lint`
- `typecheck`
- `test`
- `test:release-gate`
- `audit:frontend-ipc`
- `audit:large-ipc`
- release/build/perf/audit helper scripts

`website/package.json` contains:

- `dev`
- `prebuild`
- `build`
- `preview`
- `astro`

Website gaps:

- no dedicated `lint` script
- no dedicated `test` script
- `npm --prefix website ci` reports dependency vulnerabilities

`license-server/composer.json` exists, but:

- no `scripts` block is defined
- Composer is unavailable in the current PATH, so validate/test commands cannot run here

---

## Lint and typecheck exclusions

`eslint.config.mjs` ignores:

- `dist/**`
- `scripts/**`
- `src/rust/**/pkg/**`
- `src/rust/**/target/**`
- `src-tauri/**`
- `node_modules/**`
- `coverage/**`
- `test-results/**`
- `playwright-report/**`
- `outputs/**`
- `.next/**`
- `.agent/**`
- `.sisyphus/**`
- `website/**`
- `license-server/**`
- `runtime/**`
- `e2e/**`
- `tests/e2e/_archived/**`
- `docs/**`
- `Regents/**`

`tsconfig.json` excludes:

- `node_modules`
- `dist`
- `scripts`
- `website`
- `src-tauri`
- `tests/e2e/_archived`

---

## Security notes

- No secrets were added.
- This PR is documentation-only.
- Production npm audit passed in the baseline run with `npm audit --omit=dev`.
- Cargo audit passed in the baseline run.
- Full root `npm ci` and website `npm ci` still reported dependency vulnerabilities in broader
  dependency trees; those findings need separate dependency triage.

---

## Performance notes

- No runtime or performance behavior changed.
- Release gate performance snapshot passed during the baseline run within its configured budgets.
- `audit:frontend-ipc` timeout behavior is a tooling reliability finding, handled separately in PR #6.

---

## Risk assessment

Low. PR #5 adds one markdown baseline report and has no runtime behavior changes.

Main residual risks are process/reporting risks:

- Some baseline observations depend on the local Windows environment used for the audit.
- License-server Composer gates are not reproducible on this machine until Composer is installed
  and project scripts are defined.
- Audit runner and Clippy fixes must be reviewed in their own PRs before treating those gates as
  newly reliable.

---

## Rollback plan

Revert this PR or delete one file:

- `docs/audits/progress/2026-06-13-baseline.md`

No application runtime state, generated files, scripts, Rust source, or application behavior needs
rollback.

---

## Reviewer questions

- Should Composer installation and license-server scripts become a separate tooling PR after the
  current split sequence?
- Should dependency vulnerability triage be tracked separately from this audit split?
